[metadata]
name = what-im-buying
version = 0.1.0
description = NFC-e parser and invoice normalization

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.10

[options.packages.find]
where = src

[tool:pytest]
pythonpath = .
testpaths = tests
